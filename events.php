<h3>Events</h3>
<p> This contains the Events stuff. All this data is retrieved using AJAX call into a div box</p>
<p>So as to keep all the structure same for every page just by changint the content</p>
<p>Each Link has a page where it's data which is to be retrieved using AJAX is stored.yeah</p>
